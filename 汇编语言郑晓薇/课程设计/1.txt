buf3 buf4

Output MACRO x1					;x1= 字符串
		mov si,0				;计算字符串长度	
		local loop3	
		mov al,ds:byte ptr [x1+si]
		inc si
		cmp al,'$'
		jnz loop3
		dec si
		
		mov cx,si
		add ds:word ptr [totalLength],cx			;计算总长度
		mov bx,ds:word ptr [totalLength]			;接着存储
		mov si,0
loop4:	mov al,ds:byte ptr [x1+si]
		mov ds:byte ptr [outcome+si+bx],al
		inc si
		loop loop4	
ENDM








		mov si,0				;计算字符串长度		
loop3:	mov al,ds:byte ptr [buf2+si]
		inc si
		cmp al,'$'
		jnz loop3
		dec si
		
		mov cx,si
		mov bx,ds:word ptr [totalLength]			;接着存储
		mov si,0
loop4:	mov al,ds:byte ptr [buf2+si]
		mov ds:byte ptr [outcome+si+bx],al
		inc si
		add ds:word ptr [totalLength],1
		loop loop4



检测:	   push dx
		   push ax
           mov dx,0
           mov ax,ds:word ptr [totalLength]
           call stackdiv16
		   pop ax
		   pop dx