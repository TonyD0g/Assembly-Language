DATAS SEGMENT
    STRING DB 'This is the new line!$'
    
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    CALL COUTENTER
    LEA  DX,STRING
    MOV  AH,9
    INT  21H
    MOV AH,4CH
    INT 21H
    
COUTENTER PROC
	PUSH AX
	PUSH DX
	 ;回车换行
    MOV AH,02H
	MOV DL,0DH
	INT 21H
	MOV AH,02H
	MOV DL,0AH
	INT 21H
	POP DX
	POP AX
	RET
COUTENTER ENDP


CODES ENDS
    END START